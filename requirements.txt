# Pinned to a known-good, mutually-compatible set.
# Unpinned (>=) versions let Streamlit Cloud pull newer native wheels
# (numpy / pyarrow) that segfaulted the app on rebuild — pinning prevents that.
streamlit==1.50.0
pandas==2.3.3
numpy==2.0.2
pyarrow==21.0.0
altair==5.5.0
plotly==6.7.0
supabase==2.30.1
extra-streamlit-components==0.1.81
streamlit-calendar==1.4.0
streamlit-option-menu==0.4.0
python-docx==1.2.0
